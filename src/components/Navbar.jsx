import React from "react";
import { BrowserRouter, Routes, Route } from "react-router-dom";

const Navbar = () => {
  return (
    <div>
      <ul>
        <li>Inicio</li>
      </ul>
    </div>
  );
};

export default Navbar;
